
capture replace country="Afghanistan" if ccode1==700
capture replace country="Albania" if ccode1==339
capture replace country="Albania" if ccode1==339
capture replace country="Algeria" if ccode1==615
capture replace country="Andorra" if ccode1==232
capture replace country="Angola" if ccode1==540
capture replace country="Antigua & Barbuda" if ccode1==58
capture replace country="Argentina" if ccode1==160
capture replace country="Armenia" if ccode1==371
capture replace country="Australia" if ccode1==900
capture replace country="Austria" if ccode1==305
capture replace country="Austria" if ccode1==305
capture replace country="Austria-Hungary" if ccode1==300
capture replace country="Azerbaijan" if ccode1==373
capture replace country="Baden" if ccode1==267
capture replace country="Bahamas" if ccode1==31
capture replace country="Bahrain" if ccode1==692
capture replace country="Bangladesh" if ccode1==771
capture replace country="Barbados" if ccode1==53
capture replace country="Bavaria" if ccode1==245
capture replace country="Belarus" if ccode1==370
capture replace country="Belgium" if ccode1==211
capture replace country="Belgium" if ccode1==211
capture replace country="Belize" if ccode1==80
capture replace country="Benin" if ccode1==434
capture replace country="Bhutan" if ccode1==760
capture replace country="Bolivia" if ccode1==145
capture replace country="Bosnia and Herzegovina" if ccode1==346
capture replace country="Botswana" if ccode1==571
capture replace country="Brazil" if ccode1==140
capture replace country="Brunei" if ccode1==835
capture replace country="Bulgaria" if ccode1==355
capture replace country="Burkina Faso" if ccode1==439
capture replace country="Burundi" if ccode1==516
capture replace country="Cambodia" if ccode1==811
capture replace country="Cameroon" if ccode1==471
capture replace country="Canada" if ccode1==20
capture replace country="Cape Verde" if ccode1==402
capture replace country="Central African Republic" if ccode1==482
capture replace country="Chad" if ccode1==483
capture replace country="Chile" if ccode1==155
capture replace country="China" if ccode1==710
capture replace country="Colombia" if ccode1==100
capture replace country="Comoros" if ccode1==581
capture replace country="Congo" if ccode1==484
capture replace country="Costa Rica" if ccode1==94
capture replace country="Croatia" if ccode1==344
capture replace country="Cuba" if ccode1==40
capture replace country="Cuba" if ccode1==40
capture replace country="Cyprus" if ccode1==352
capture replace country="Czech Republic" if ccode1==316
capture replace country="Czechoslovakia" if ccode1==315
capture replace country="Czechoslovakia" if ccode1==315
capture replace country="Democratic Republic of the Congo" if ccode1==490
capture replace country="Denmark" if ccode1==390
capture replace country="Denmark" if ccode1==390
capture replace country="Djibouti" if ccode1==522
capture replace country="Dominica" if ccode1==54
capture replace country="Dominican Republic" if ccode1==42
capture replace country="Dominican Republic" if ccode1==42
capture replace country="East Timor" if ccode1==860
capture replace country="Ecuador" if ccode1==130
capture replace country="Egypt" if ccode1==651
capture replace country="Egypt" if ccode1==651
capture replace country="El Salvador" if ccode1==92
capture replace country="Equatorial Guinea" if ccode1==411
capture replace country="Eritrea" if ccode1==531
capture replace country="Estonia" if ccode1==366
capture replace country="Estonia" if ccode1==366
capture replace country="Ethiopia" if ccode1==530
capture replace country="Ethiopia" if ccode1==530
capture replace country="Federated States of Micronesia" if ccode1==987
capture replace country="Fiji" if ccode1==950
capture replace country="Finland" if ccode1==375
capture replace country="France" if ccode1==220
capture replace country="France" if ccode1==220
capture replace country="Gabon" if ccode1==481
capture replace country="Gambia" if ccode1==420
capture replace country="Georgia" if ccode1==372
capture replace country="German Democratic Republic" if ccode1==265
capture replace country="German Federal Republic" if ccode1==260
capture replace country="Germany" if ccode1==255
capture replace country="Germany" if ccode1==255
capture replace country="Ghana" if ccode1==452
capture replace country="Greece" if ccode1==350
capture replace country="Greece" if ccode1==350
capture replace country="Grenada" if ccode1==55
capture replace country="Guatemala" if ccode1==90
capture replace country="Guinea" if ccode1==438
capture replace country="Guinea-Bissau" if ccode1==404
capture replace country="Guyana" if ccode1==110
capture replace country="Haiti" if ccode1==41
capture replace country="Haiti" if ccode1==41
capture replace country="Hanover" if ccode1==240
capture replace country="Hesse Electoral" if ccode1==273
capture replace country="Hesse Grand Ducal" if ccode1==275
capture replace country="Honduras" if ccode1==91
capture replace country="Hungary" if ccode1==310
capture replace country="Iceland" if ccode1==395
capture replace country="India" if ccode1==750
capture replace country="Indonesia" if ccode1==850
capture replace country="Iran" if ccode1==630
capture replace country="Iraq" if ccode1==645
capture replace country="Ireland" if ccode1==205
capture replace country="Israel" if ccode1==666
capture replace country="Italy" if ccode1==325
capture replace country="Ivory Coast" if ccode1==437
capture replace country="Jamaica" if ccode1==51
capture replace country="Japan" if ccode1==740
capture replace country="Japan" if ccode1==740
capture replace country="Jordan" if ccode1==663
capture replace country="Kazakhstan" if ccode1==705
capture replace country="Kenya" if ccode1==501
capture replace country="Kiribati" if ccode1==946
capture replace country="Korea" if ccode1==730
capture replace country="Kuwait" if ccode1==690
capture replace country="Kyrgyzstan" if ccode1==703
capture replace country="Laos" if ccode1==812
capture replace country="Latvia" if ccode1==367
capture replace country="Latvia" if ccode1==367
capture replace country="Lebanon" if ccode1==660
capture replace country="Lesotho" if ccode1==570
capture replace country="Liberia" if ccode1==450
capture replace country="Libya" if ccode1==620
capture replace country="Liechtenstein" if ccode1==223
capture replace country="Lithuania" if ccode1==368
capture replace country="Lithuania" if ccode1==368
capture replace country="Luxembourg" if ccode1==212
capture replace country="Luxembourg" if ccode1==212
capture replace country="Macedonia" if ccode1==343
capture replace country="Madagascar" if ccode1==580
capture replace country="Malawi" if ccode1==553
capture replace country="Malaysia" if ccode1==820
capture replace country="Maldives" if ccode1==781
capture replace country="Mali" if ccode1==432
capture replace country="Malta" if ccode1==338
capture replace country="Marshall Islands" if ccode1==983
capture replace country="Mauritania" if ccode1==435
capture replace country="Mauritius" if ccode1==590
capture replace country="Mecklenburg Schwerin" if ccode1==280
capture replace country="Mexico" if ccode1==70
capture replace country="Modena" if ccode1==332
capture replace country="Moldova" if ccode1==359
capture replace country="Monaco" if ccode1==221
capture replace country="Mongolia" if ccode1==712
capture replace country="Morocco" if ccode1==600
capture replace country="Morocco" if ccode1==600
capture replace country="Mozambique" if ccode1==541
capture replace country="Myanmar" if ccode1==775
capture replace country="Namibia" if ccode1==565
capture replace country="Nauru" if ccode1==970
capture replace country="Nepal" if ccode1==790
capture replace country="Netherlands" if ccode1==210
capture replace country="Netherlands" if ccode1==210
capture replace country="New Zealand" if ccode1==920
capture replace country="Nicaragua" if ccode1==93
capture replace country="Niger" if ccode1==436
capture replace country="Nigeria" if ccode1==475
capture replace country="North Korea" if ccode1==731
capture replace country="Norway" if ccode1==385
capture replace country="Norway" if ccode1==385
capture replace country="Oman" if ccode1==698
capture replace country="Pakistan" if ccode1==770
capture replace country="Palau" if ccode1==986
capture replace country="Panama" if ccode1==95
capture replace country="Papal States" if ccode1==327
capture replace country="Papua New Guinea" if ccode1==910
capture replace country="Paraguay" if ccode1==150
capture replace country="Paraguay" if ccode1==150
capture replace country="Parma" if ccode1==335
capture replace country="Peru" if ccode1==135
capture replace country="Philippines" if ccode1==840
capture replace country="Poland" if ccode1==290
capture replace country="Poland" if ccode1==290
capture replace country="Portugal" if ccode1==235
capture replace country="Qatar" if ccode1==694
capture replace country="Republic of Vietnam" if ccode1==817
capture replace country="Romania" if ccode1==360
capture replace country="Russia" if ccode1==365
capture replace country="Rwanda" if ccode1==517
capture replace country="Samoa" if ccode1==990
capture replace country="San Marino" if ccode1==331
capture replace country="Sao Tome and Principe" if ccode1==403
capture replace country="Saudi Arabia" if ccode1==670
capture replace country="Saxony" if ccode1==269
capture replace country="Senegal" if ccode1==433
capture replace country="Seychelles" if ccode1==591
capture replace country="Sierra Leone" if ccode1==451
capture replace country="Singapore" if ccode1==830
capture replace country="Slovakia" if ccode1==317
capture replace country="Slovenia" if ccode1==349
capture replace country="Solomon Islands" if ccode1==940
capture replace country="Somalia" if ccode1==520
capture replace country="South Africa" if ccode1==560
capture replace country="South Korea" if ccode1==732
capture replace country="Spain" if ccode1==230
capture replace country="Sri Lanka" if ccode1==780
capture replace country="St. Kitts and Nevis" if ccode1==60
capture replace country="St. Lucia" if ccode1==56
capture replace country="St. Vincent and the Grenadines" if ccode1==57
capture replace country="Sudan" if ccode1==625
capture replace country="Suriname" if ccode1==115
capture replace country="Swaziland" if ccode1==572
capture replace country="Sweden" if ccode1==380
capture replace country="Switzerland" if ccode1==225
capture replace country="Syria" if ccode1==652
capture replace country="Syria" if ccode1==652
capture replace country="Taiwan" if ccode1==713
capture replace country="Tajikistan" if ccode1==702
capture replace country="Tanzania" if ccode1==510
capture replace country="Thailand" if ccode1==800
capture replace country="Togo" if ccode1==461
capture replace country="Tonga" if ccode1==955
capture replace country="Trinidad and Tobago" if ccode1==52
capture replace country="Tunisia" if ccode1==616
capture replace country="Tunisia" if ccode1==616
capture replace country="Turkey" if ccode1==640
capture replace country="Turkmenistan" if ccode1==701
capture replace country="Tuscany" if ccode1==337
capture replace country="Tuvalu" if ccode1==947
capture replace country="Two Sicilies" if ccode1==329
capture replace country="Uganda" if ccode1==500
capture replace country="Ukraine" if ccode1==369
capture replace country="United Arab Emirates" if ccode1==696
capture replace country="United Kingdom" if ccode1==200
capture replace country="United States of America" if ccode1==2
capture replace country="Uruguay" if ccode1==165
capture replace country="Uzbekistan" if ccode1==704
capture replace country="Vanuatu" if ccode1==935
capture replace country="Venezuela" if ccode1==101
capture replace country="Vietnam" if ccode1==816
capture replace country="Wuerttemburg" if ccode1==271
capture replace country="Yemen" if ccode1==679
capture replace country="Yemen Arab Republic" if ccode1==678
capture replace country="Yemen People's Republic" if ccode1==680
capture replace country="Yugoslavia" if ccode1==345
capture replace country="Yugoslavia" if ccode1==345
capture replace country="Zambia" if ccode1==551
capture replace country="Zanzibar" if ccode1==511
capture replace country="Zimbabwe" if ccode1==552


capture replace partner="Afghanistan" if ccode2==700
capture replace partner="Albania" if ccode2==339
capture replace partner="Albania" if ccode2==339
capture replace partner="Algeria" if ccode2==615
capture replace partner="Andorra" if ccode2==232
capture replace partner="Angola" if ccode2==540
capture replace partner="Antigua & Barbuda" if ccode2==58
capture replace partner="Argentina" if ccode2==160
capture replace partner="Armenia" if ccode2==371
capture replace partner="Australia" if ccode2==900
capture replace partner="Austria" if ccode2==305
capture replace partner="Austria" if ccode2==305
capture replace partner="Austria-Hungary" if ccode2==300
capture replace partner="Azerbaijan" if ccode2==373
capture replace partner="Baden" if ccode2==267
capture replace partner="Bahamas" if ccode2==31
capture replace partner="Bahrain" if ccode2==692
capture replace partner="Bangladesh" if ccode2==771
capture replace partner="Barbados" if ccode2==53
capture replace partner="Bavaria" if ccode2==245
capture replace partner="Belarus" if ccode2==370
capture replace partner="Belgium" if ccode2==211
capture replace partner="Belgium" if ccode2==211
capture replace partner="Belize" if ccode2==80
capture replace partner="Benin" if ccode2==434
capture replace partner="Bhutan" if ccode2==760
capture replace partner="Bolivia" if ccode2==145
capture replace partner="Bosnia and Herzegovina" if ccode2==346
capture replace partner="Botswana" if ccode2==571
capture replace partner="Brazil" if ccode2==140
capture replace partner="Brunei" if ccode2==835
capture replace partner="Bulgaria" if ccode2==355
capture replace partner="Burkina Faso" if ccode2==439
capture replace partner="Burundi" if ccode2==516
capture replace partner="Cambodia" if ccode2==811
capture replace partner="Cameroon" if ccode2==471
capture replace partner="Canada" if ccode2==20
capture replace partner="Cape Verde" if ccode2==402
capture replace partner="Central African Republic" if ccode2==482
capture replace partner="Chad" if ccode2==483
capture replace partner="Chile" if ccode2==155
capture replace partner="China" if ccode2==710
capture replace partner="Colombia" if ccode2==100
capture replace partner="Comoros" if ccode2==581
capture replace partner="Congo" if ccode2==484
capture replace partner="Costa Rica" if ccode2==94
capture replace partner="Croatia" if ccode2==344
capture replace partner="Cuba" if ccode2==40
capture replace partner="Cuba" if ccode2==40
capture replace partner="Cyprus" if ccode2==352
capture replace partner="Czech Republic" if ccode2==316
capture replace partner="Czechoslovakia" if ccode2==315
capture replace partner="Czechoslovakia" if ccode2==315
capture replace partner="Democratic Republic of the Congo" if ccode2==490
capture replace partner="Denmark" if ccode2==390
capture replace partner="Denmark" if ccode2==390
capture replace partner="Djibouti" if ccode2==522
capture replace partner="Dominica" if ccode2==54
capture replace partner="Dominican Republic" if ccode2==42
capture replace partner="Dominican Republic" if ccode2==42
capture replace partner="East Timor" if ccode2==860
capture replace partner="Ecuador" if ccode2==130
capture replace partner="Egypt" if ccode2==651
capture replace partner="Egypt" if ccode2==651
capture replace partner="El Salvador" if ccode2==92
capture replace partner="Equatorial Guinea" if ccode2==411
capture replace partner="Eritrea" if ccode2==531
capture replace partner="Estonia" if ccode2==366
capture replace partner="Estonia" if ccode2==366
capture replace partner="Ethiopia" if ccode2==530
capture replace partner="Ethiopia" if ccode2==530
capture replace partner="Federated States of Micronesia" if ccode2==987
capture replace partner="Fiji" if ccode2==950
capture replace partner="Finland" if ccode2==375
capture replace partner="France" if ccode2==220
capture replace partner="France" if ccode2==220
capture replace partner="Gabon" if ccode2==481
capture replace partner="Gambia" if ccode2==420
capture replace partner="Georgia" if ccode2==372
capture replace partner="German Democratic Republic" if ccode2==265
capture replace partner="German Federal Republic" if ccode2==260
capture replace partner="Germany" if ccode2==255
capture replace partner="Germany" if ccode2==255
capture replace partner="Ghana" if ccode2==452
capture replace partner="Greece" if ccode2==350
capture replace partner="Greece" if ccode2==350
capture replace partner="Grenada" if ccode2==55
capture replace partner="Guatemala" if ccode2==90
capture replace partner="Guinea" if ccode2==438
capture replace partner="Guinea-Bissau" if ccode2==404
capture replace partner="Guyana" if ccode2==110
capture replace partner="Haiti" if ccode2==41
capture replace partner="Haiti" if ccode2==41
capture replace partner="Hanover" if ccode2==240
capture replace partner="Hesse Electoral" if ccode2==273
capture replace partner="Hesse Grand Ducal" if ccode2==275
capture replace partner="Honduras" if ccode2==91
capture replace partner="Hungary" if ccode2==310
capture replace partner="Iceland" if ccode2==395
capture replace partner="India" if ccode2==750
capture replace partner="Indonesia" if ccode2==850
capture replace partner="Iran" if ccode2==630
capture replace partner="Iraq" if ccode2==645
capture replace partner="Ireland" if ccode2==205
capture replace partner="Israel" if ccode2==666
capture replace partner="Italy" if ccode2==325
capture replace partner="Ivory Coast" if ccode2==437
capture replace partner="Jamaica" if ccode2==51
capture replace partner="Japan" if ccode2==740
capture replace partner="Japan" if ccode2==740
capture replace partner="Jordan" if ccode2==663
capture replace partner="Kazakhstan" if ccode2==705
capture replace partner="Kenya" if ccode2==501
capture replace partner="Kiribati" if ccode2==946
capture replace partner="Korea" if ccode2==730
capture replace partner="Kuwait" if ccode2==690
capture replace partner="Kyrgyzstan" if ccode2==703
capture replace partner="Laos" if ccode2==812
capture replace partner="Latvia" if ccode2==367
capture replace partner="Latvia" if ccode2==367
capture replace partner="Lebanon" if ccode2==660
capture replace partner="Lesotho" if ccode2==570
capture replace partner="Liberia" if ccode2==450
capture replace partner="Libya" if ccode2==620
capture replace partner="Liechtenstein" if ccode2==223
capture replace partner="Lithuania" if ccode2==368
capture replace partner="Lithuania" if ccode2==368
capture replace partner="Luxembourg" if ccode2==212
capture replace partner="Luxembourg" if ccode2==212
capture replace partner="Macedonia" if ccode2==343
capture replace partner="Madagascar" if ccode2==580
capture replace partner="Malawi" if ccode2==553
capture replace partner="Malaysia" if ccode2==820
capture replace partner="Maldives" if ccode2==781
capture replace partner="Mali" if ccode2==432
capture replace partner="Malta" if ccode2==338
capture replace partner="Marshall Islands" if ccode2==983
capture replace partner="Mauritania" if ccode2==435
capture replace partner="Mauritius" if ccode2==590
capture replace partner="Mecklenburg Schwerin" if ccode2==280
capture replace partner="Mexico" if ccode2==70
capture replace partner="Modena" if ccode2==332
capture replace partner="Moldova" if ccode2==359
capture replace partner="Monaco" if ccode2==221
capture replace partner="Mongolia" if ccode2==712
capture replace partner="Morocco" if ccode2==600
capture replace partner="Morocco" if ccode2==600
capture replace partner="Mozambique" if ccode2==541
capture replace partner="Myanmar" if ccode2==775
capture replace partner="Namibia" if ccode2==565
capture replace partner="Nauru" if ccode2==970
capture replace partner="Nepal" if ccode2==790
capture replace partner="Netherlands" if ccode2==210
capture replace partner="Netherlands" if ccode2==210
capture replace partner="New Zealand" if ccode2==920
capture replace partner="Nicaragua" if ccode2==93
capture replace partner="Niger" if ccode2==436
capture replace partner="Nigeria" if ccode2==475
capture replace partner="North Korea" if ccode2==731
capture replace partner="Norway" if ccode2==385
capture replace partner="Norway" if ccode2==385
capture replace partner="Oman" if ccode2==698
capture replace partner="Pakistan" if ccode2==770
capture replace partner="Palau" if ccode2==986
capture replace partner="Panama" if ccode2==95
capture replace partner="Papal States" if ccode2==327
capture replace partner="Papua New Guinea" if ccode2==910
capture replace partner="Paraguay" if ccode2==150
capture replace partner="Paraguay" if ccode2==150
capture replace partner="Parma" if ccode2==335
capture replace partner="Peru" if ccode2==135
capture replace partner="Philippines" if ccode2==840
capture replace partner="Poland" if ccode2==290
capture replace partner="Poland" if ccode2==290
capture replace partner="Portugal" if ccode2==235
capture replace partner="Qatar" if ccode2==694
capture replace partner="Republic of Vietnam" if ccode2==817
capture replace partner="Romania" if ccode2==360
capture replace partner="Russia" if ccode2==365
capture replace partner="Rwanda" if ccode2==517
capture replace partner="Samoa" if ccode2==990
capture replace partner="San Marino" if ccode2==331
capture replace partner="Sao Tome and Principe" if ccode2==403
capture replace partner="Saudi Arabia" if ccode2==670
capture replace partner="Saxony" if ccode2==269
capture replace partner="Senegal" if ccode2==433
capture replace partner="Seychelles" if ccode2==591
capture replace partner="Sierra Leone" if ccode2==451
capture replace partner="Singapore" if ccode2==830
capture replace partner="Slovakia" if ccode2==317
capture replace partner="Slovenia" if ccode2==349
capture replace partner="Solomon Islands" if ccode2==940
capture replace partner="Somalia" if ccode2==520
capture replace partner="South Africa" if ccode2==560
capture replace partner="South Korea" if ccode2==732
capture replace partner="Spain" if ccode2==230
capture replace partner="Sri Lanka" if ccode2==780
capture replace partner="St. Kitts and Nevis" if ccode2==60
capture replace partner="St. Lucia" if ccode2==56
capture replace partner="St. Vincent and the Grenadines" if ccode2==57
capture replace partner="Sudan" if ccode2==625
capture replace partner="Suriname" if ccode2==115
capture replace partner="Swaziland" if ccode2==572
capture replace partner="Sweden" if ccode2==380
capture replace partner="Switzerland" if ccode2==225
capture replace partner="Syria" if ccode2==652
capture replace partner="Syria" if ccode2==652
capture replace partner="Taiwan" if ccode2==713
capture replace partner="Tajikistan" if ccode2==702
capture replace partner="Tanzania" if ccode2==510
capture replace partner="Thailand" if ccode2==800
capture replace partner="Togo" if ccode2==461
capture replace partner="Tonga" if ccode2==955
capture replace partner="Trinidad and Tobago" if ccode2==52
capture replace partner="Tunisia" if ccode2==616
capture replace partner="Tunisia" if ccode2==616
capture replace partner="Turkey" if ccode2==640
capture replace partner="Turkmenistan" if ccode2==701
capture replace partner="Tuscany" if ccode2==337
capture replace partner="Tuvalu" if ccode2==947
capture replace partner="Two Sicilies" if ccode2==329
capture replace partner="Uganda" if ccode2==500
capture replace partner="Ukraine" if ccode2==369
capture replace partner="United Arab Emirates" if ccode2==696
capture replace partner="United Kingdom" if ccode2==200
capture replace partner="United States of America" if ccode2==2
capture replace partner="Uruguay" if ccode2==165
capture replace partner="Uzbekistan" if ccode2==704
capture replace partner="Vanuatu" if ccode2==935
capture replace partner="Venezuela" if ccode2==101
capture replace partner="Vietnam" if ccode2==816
capture replace partner="Wuerttemburg" if ccode2==271
capture replace partner="Yemen" if ccode2==679
capture replace partner="Yemen Arab Republic" if ccode2==678
capture replace partner="Yemen People's Republic" if ccode2==680
capture replace partner="Yugoslavia" if ccode2==345
capture replace partner="Yugoslavia" if ccode2==345
capture replace partner="Zambia" if ccode2==551
capture replace partner="Zanzibar" if ccode2==511
capture replace partner="Zimbabwe" if ccode2==552

